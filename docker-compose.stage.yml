version: '3'
services:
    expub_enterprise_stage:
        build:
            context: .
            dockerfile: Dockerfile
            args:
                - NODE_ENV=stage
        restart: always
        ports:
            - '9080:9080'
